spring:
  application:
    name: bytebankers
  datasource:
    driver-class-name: org.postgresql.Driver
    password: 1234
    url: jdbc:postgresql://localhost:5432/bytebankers
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080